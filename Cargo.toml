[package]
name = "blazingice_bevy"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.19", features = ["webgl2", "pbr_multi_layer_material_textures"] }
wasm-bindgen = "0.2"

[profile.release]
opt-level = "z"
lto = false
codegen-units = 16
panic = "abort"
strip = true
